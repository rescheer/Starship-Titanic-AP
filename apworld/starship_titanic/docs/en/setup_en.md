# Starship Titanic Setup Guide (placeholder)

This is a first-pass, logic-only Archipelago world. There is no client mod
yet, so there is nothing to "play" against a real copy of the game through
Archipelago at this stage -- this file exists only so the `Tutorial` entry
in `__init__.py` doesn't point at a missing document.

Once a client mod exists that can send/receive checks against the running
game, replace this file with real setup instructions (installing the mod,
connecting to a server, etc.).
